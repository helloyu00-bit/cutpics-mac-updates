# Cutpics Mac Updates

Public update manifest for Cutpics for Mac.

The app reads `latest.json` to discover new versions. App source code can remain private; this repository only hosts public update metadata and release download links.
